## Simple training demo for SISR with MSRResNet

```python
python main_train_psnr.py --opt options/train_msrresnet_psnr.json
```

or use the run.sh to specify the device(s) through

```bash
bash run.sh
```